# Swift / SwiftPM
.build/
build/
.swiftpm/
Package.resolved
# PupaHost pins remote SPM versions for reproducible builds — keep this one.
!PupaHost/PupaHost.xcodeproj/project.xcworkspace/xcshareddata/swiftpm/Package.resolved

# Local signing override — holds each developer's DEVELOPMENT_TEAM, never committed.
PupaHost/Local.xcconfig

# Xcode user state
**/xcuserdata/
*.xcuserstate
*.xcscmblueprint
*.xccheckout

# Swift compiler intermediates (sometimes emitted to project root)
*.d
*.dia
*.swiftdeps

# Editor / OS
.DS_Store
.idea/
.vscode/
.claude/*
!.claude/skills/
*.swp

skills-lock.json
.agents
.claude/skills/app-store-review