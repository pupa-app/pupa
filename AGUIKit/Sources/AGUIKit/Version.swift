/// Version of the AGUIKit Swift package.
///
/// Bumps follow patch-only semantics within the pupa project (only the
/// last component changes unless explicitly told otherwise). Increment on any
/// API-visible change to AGUIKit.
public let AGUIKitVersion: String = "0.0.23"
