/// Version of the Pupa iOS / macOS app.
///
/// Bumps follow patch-only semantics within the pupa project (only the
/// last component changes unless explicitly told otherwise).
public let PupaAppVersion: String = "0.0.208"
